# from django.urls import path
#
# from item_tag.views import ListItemView, SearchItemView, RetrieveUpdateDestroyItemView

urlpatterns = [
    # backend/api/item_tags/
    # path('', ListItemTagView.as_view()),
    # path('new/', CreateItemTagView.as_view()),
    # path('search/', SearchItemTagView.as_view()),
    # path('<int:item_tag_id>/', RetrieveUpdateDestroyItemView.as_view())
]
